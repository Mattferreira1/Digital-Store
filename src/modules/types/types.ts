export type User = {
  name: string;
  cpf: string;
  email: string;
  phoneNumber: string;
  address: string;
  neighborhood: string;
  city: string;
  zipCode: string;
  complement: string;
};

export type LoginInputs = {
  email: string;
  password: string;
};
